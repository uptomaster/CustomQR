# CustomQR

Dense pixel-style QR in the browser, with a WebGL 3D block preview. Export scans as 2D PNG.

```bash
npm install
npm run dev
# npm run build
```

Stack: Vite, React, `qrcode-generator`, Three (react-three-fiber + drei).
